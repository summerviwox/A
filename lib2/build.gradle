plugins {
    id 'java-library'
}

dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
    api(libs.com.google.auto.service)
    //annotationProcessor(libs.com.google.auto.service)
    implementation(libs.com.squareup.javapoet)
}
